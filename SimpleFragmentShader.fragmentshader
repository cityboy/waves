#version 330 core

// Interpolated values from the vertex shaders
in vec3	 normal;
out vec3 color;
 
void main() {
	// Simple Lambertian reflectance model
	color = clamp(vec3(0.2,0.4,0.6) * max(dot(normal,vec3(1.0,1.0,1.0)),0.0), 0.0, 1.0);
}

